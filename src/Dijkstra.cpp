#include "Dijkstra.h"

NodeRecord Dijkstra::GetSteering(Graph i_WorldGraph, NodeRecord i_Start, NodeRecord i_End)
{

	auto startRecord = i_Start;
	std::vector<NodeRecord> openList, closeList;
	openList.push_back(startRecord);

	while (!openList.empty())
	{
		
	}


}
